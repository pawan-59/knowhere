# Optional: expose the dashboard over HTTPS. Requires an ingress controller and
# (for TLS) cert-manager or a pre-provisioned certificate. Adjust host/class.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: knowhere
  labels:
    app: knowhere
  annotations:
    # cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - knowhere.example.com
      secretName: knowhere-tls
  rules:
    - host: knowhere.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: knowhere
                port:
                  number: 80
