apiVersion: v1
kind: Service
metadata:
  name: central-devtron
  labels:
    app: central-devtron
spec:
  type: ClusterIP
  selector:
    app: central-devtron
  ports:
    - name: http
      port: 80
      targetPort: http
